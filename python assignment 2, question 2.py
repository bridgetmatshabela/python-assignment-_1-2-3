class Shape:
    def_init_(self,color,area)
    self.color = color

    def area(self):
        pass
    class Circle(Shape):
        def_init_(self,color,radius)
        super()._init_(color)
        self.radius = radius

        def area(self):
            return 3.14
            self.radius**2

            class rectangle(Shape):
                def_init_(self,color,side_length,side_width)
                super()._init_(color)
                self.side_length = 2
                self.side_width = 1

                def area (self):
                    return
                self.side_lenghth * width

                print(circle. area)
                print(rectangle.area)


